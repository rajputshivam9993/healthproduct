import { PagePlaceholder } from '@/components/page-placeholder';

/** Analytics: charts for trends, revenue, doctor performance, growth (Req 19.5). */
export function AnalyticsPage() {
  return (
    <PagePlaceholder
      title="Analytics"
      description="Recharts-based metrics: appointment trends, revenue, growth."
    />
  );
}
